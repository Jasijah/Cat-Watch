# Info.plist keys to add

- `NSLocationWhenInUseUsageDescription` = "CATWatch uses your location to show nearby catastrophes and zone-based alerts while you are using the app."
- `NSUserNotificationsUsageDescription` = "CATWatch sends alerts for catastrophic incidents that match your watch zones and severity rules."
- `UIBackgroundModes` (optional for future remote/background processing) includes `fetch` when implemented.
